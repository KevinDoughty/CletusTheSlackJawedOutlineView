This sample shows NSOutlineView preservation of selection and expansion state across saves and undo/redo.
It uses an NSTreeController with a custom data model, not Core Data.
Unfortunately it makes drag and drop difficult.
